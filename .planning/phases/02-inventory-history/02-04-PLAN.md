---
phase: 02-inventory-history
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
gap_closure: true
files_modified:
  - src/data/db.ts
  - src/features/timer/TimerScreen.tsx
autonomous: true

must_haves:
  truths:
    - "Inventory screen is reachable from the main timer screen."
    - "Lots can be listed without Dexie index errors."
  artifacts:
    - path: "src/data/db.ts"
      provides: "Dexie schema includes lots.createdAt index"
      contains: "createdAt"
    - path: "src/features/timer/TimerScreen.tsx"
      provides: "Navigation link to /inventory"
      contains: "Inventory"
---

<objective>
Fix inventory navigation and Dexie schema index errors.

Purpose: Resolve user-reported runtime errors and unblock inventory access from the main screen.
Output: Inventory link in the main header and lots store indexed by createdAt.
</objective>

<execution_context>
@/Users/bobsmachine/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/bobsmachine/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-inventory-history/02-inventory-history-VERIFICATION.md
@src/data/db.ts
@src/features/timer/TimerScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createdAt index for lots</name>
  <files>
    src/data/db.ts
  </files>
  <action>
    Bump Dexie schema to a new version and include `createdAt` in the lots store indexes so `orderBy("createdAt")` works without schema errors. Keep existing indexes intact.
  </action>
  <verify>npm run lint</verify>
  <done>Lots list queries run without `KeyPath createdAt` index errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add inventory navigation from timer screen</name>
  <files>
    src/features/timer/TimerScreen.tsx
  </files>
  <action>
    Add a visible navigation link to `/inventory` alongside the existing History link in the timer header. Keep the visual style consistent with other header links.
  </action>
  <verify>npm run lint</verify>
  <done>Main timer screen shows a link to the inventory screen.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- Open the app, click Inventory from the timer header, and confirm `/inventory` loads without console schema errors.
</verification>

<success_criteria>
- Inventory navigation is visible on the main timer screen.
- Dexie lots queries no longer throw missing index errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-inventory-history/02-04-SUMMARY.md`
</output>
